pipeline {
    agent any

    environment {
        NER_VERSION = '1.5.0'
    }

    stages {
        stage("build") {
            steps {
                echo "building the application..."
            }
        }

        stage("test") {
            steps {
                echo "testing the application..."
            }
        }

        stage("deploy") {
            steps {
                echo "testing the application..."
            }
        }
    }
}